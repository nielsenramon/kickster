machine:
  environment:
    USER_NAME: nielsenramon
    USER_EMAIL: hello@nielsenramon.com
  ruby:
    version: 2.4.3

dependencies:
  pre:
    - ./bin/setup

test:
  post:
    - bundle exec jekyll build

deployment:
  production:
    branch: website
    commands:
      - ./bin/automated
